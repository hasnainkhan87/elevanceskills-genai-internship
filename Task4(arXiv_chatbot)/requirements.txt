# Core LangChain (same pinned band as Task 1/3 — avoids the 1.x breaking changes)
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-core>=0.3,<0.4

# Local, open-source embeddings + LLM (no API key, no quota)
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.2.0

# Vector store
faiss-cpu==1.8.0

# App
streamlit>=1.38.0
pandas>=2.0.0

# NOTE: no google-generativeai / langchain-google-genai here — Task 4
# deliberately uses an open-source LLM instead of Gemini, per the task
# brief. No .env / API key is required to run this task.
